---
title: "PreprocessingFilter.median"
linktitle: "median"
articleTitle: "median"
second_title: "Aspose.OCR for Python via Java"
description: "The median filter run through each element of the image and replace each pixel with the median of its neighboring pixels."
type: docs
weight: 10
url: /python-java/models/preprocessingfilter/median/
---

## PreprocessingFilter.median

```python
median()
```


The median filter run through each element of the image and replace each pixel with the median of its neighboring pixels.

