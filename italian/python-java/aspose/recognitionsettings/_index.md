---
title: "RecognitionSettings"
second_title: "Riferimento API di Aspose.OCR per Python via Java"
description: 
type: docs
weight: 191
url: /it/python-java/aspose/recognitionsettings/
---

Modulo recognitionsettings
==========================

Classi
-------

`CarPlateRecognitionSettings()`
:

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`IDCardRecognitionSettings()`
:

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Rileva immagini con testo bianco su sfondo scuro/nero e sceglie automaticamente un algoritmo OCR speciale per esse.
@param automaticColorInversion: contiene valore booleano - automaticColorInversion è impostato. True per impostazione predefinita.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Consente di utilizzare algoritmi aggiuntivi specifici per il riconoscimento di caratteri piccoli.
Utile per immagini con caratteri di piccole dimensioni.
@param upscaleSmallFont: contiene valore booleano - upscaleSmallFont è impostato.

`InvoiceRecognitionSettings()`
:

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Rileva immagini con testo bianco su sfondo scuro/nero e sceglie automaticamente un algoritmo OCR speciale per esse.
@param automaticColorInversion: contiene valore booleano - automaticColorInversion è impostato. True per impostazione predefinita.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Consente di utilizzare algoritmi aggiuntivi specifici per il riconoscimento di caratteri piccoli.
Utile per immagini con caratteri di piccole dimensioni.
@param upscaleSmallFont: contiene valore booleano - upscaleSmallFont è impostato.

`PassportRecognitionSettings()`
:

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Rileva immagini con testo bianco su sfondo scuro/nero e sceglie automaticamente un algoritmo OCR speciale per esse.
@param automaticColorInversion: contiene valore booleano - automaticColorInversion è impostato. True per impostazione predefinita.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Consente di utilizzare algoritmi aggiuntivi specifici per il riconoscimento di caratteri piccoli.
Utile per immagini con caratteri di piccole dimensioni.
@param upscaleSmallFont: contiene valore booleano - upscaleSmallFont è impostato.

`ReceiptRecognitionSettings()`
:

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Rileva immagini con testo bianco su sfondo scuro/nero e sceglie automaticamente un algoritmo OCR speciale per esse.
@param automaticColorInversion: contiene valore booleano - automaticColorInversion è impostato. True per impostazione predefinita.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Consente di utilizzare algoritmi aggiuntivi specifici per il riconoscimento di caratteri piccoli.
Utile per immagini con caratteri di piccole dimensioni.
@param upscaleSmallFont: contiene valore booleano - upscaleSmallFont è impostato.

`RecognitionSettings()`
:
Impostazioni per il riconoscimento dell'immagine.
Contiene elementi che consentono di personalizzare il processo di riconoscimento.
    
    
Costruttore predefinito: imposta recognitionAreas a null, linesFiltration a false, autoSkew a false, recognizeSingleLine a false.

### Variabili di classe

`JAVA_CLASS_NAME`
:

### Metodi

`set_allowed_characters(self, allowedCharacters: str)`
:
Set di caratteri consentiti. Determina l'array di caratteri consentiti per il risultato del riconoscimento.
@param allowedCharacters: contiene una stringa di caratteri.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Rileva immagini con testo bianco su sfondo scuro/nero e sceglie automaticamente un algoritmo OCR speciale per esse.
@param automaticColorInversion: contiene valore booleano - automaticColorInversion è impostato. True per impostazione predefinita.

`set_detect_areas_mode(self, detectAreasMode: aspose.models.DetectAreasMode)`
:
Determina il tipo di rete neurale utilizzata per il rilevamento delle aree.
@param detectAreasMode: contiene il valore enum DetectAreasMode.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Imposta la blacklist per i simboli di riconoscimento.
@param ignoredCharacters: Caratteri esclusi dal riconoscimento.

`set_language(self, language: aspose.models.Language)`
:
Imposta la lingua utilizzata per l'OCR.
Multilingua (nessuna) per impostazione predefinita.
@param language: contiene il valore enum Language.

`set_recognize_single_line(self, recognizeSingleLine: bool)`
:
Imposta il riconoscimento di immagini a riga singola.
Disabilitato (false) per impostazione predefinita.
Disabilita tutti i passaggi di elaborazione associati alla suddivisione in righe.
Imposta questo parametro a true se l'immagine contiene una sola riga. Disabilita le impostazioni set_recognition_areas, quindi tutte le impostazioni delle aree verranno ignorate.
@param recognizeSingleLine: True per immagine a riga singola

`set_threads_count(self, threadsCount: int)`
:
Ottiene o imposta il numero di thread per l'elaborazione.
Per impostazione predefinita, 0 significa che l'immagine verrà elaborata con un numero di thread pari al numero dei tuoi processori.
ThreadsCount = 1 significa che l'immagine verrà elaborata nel thread principale.
@param threadsCount: il numero di thread che verranno creati per il riconoscimento parallelo dei frammenti di immagine.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Consente di utilizzare algoritmi aggiuntivi specifici per il riconoscimento di caratteri piccoli.
Utile per immagini con caratteri di piccole dimensioni.
@param upscaleSmallFont: contiene valore booleano - upscaleSmallFont è impostato.



### Vedi anche

* namespace [aspose](/ocr/python-java/aspose/)
* assembly [Aspose](/ocr/python-java/)