---
title: "RecognitionSettings"
second_title: "Aspose.OCR untuk Python melalui Java API Reference"
description: 
type: docs
weight: 191
url: /id/python-java/aspose/recognitionsettings/
---

Modul recognitionsettings
==========================

Kelas
-------

`CarPlateRecognitionSettings()`
:

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`IDCardRecognitionSettings()`
:

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Mendeteksi gambar dengan teks putih pada latar belakang gelap/hitam dan secara otomatis memilih algoritma OCR khusus untuknya.
@param automaticColorInversion: berisi nilai boolean - sebuah automaticColorInversion diatur. True secara default.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Memungkinkan Anda menggunakan algoritma tambahan khusus untuk pengenalan font kecil.
Berguna untuk gambar dengan karakter berukuran kecil.
@param upscaleSmallFont: berisi nilai boolean - sebuah upscaleSmallFont diatur.

`InvoiceRecognitionSettings()`
:

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Mendeteksi gambar dengan teks putih pada latar belakang gelap/hitam dan secara otomatis memilih algoritma OCR khusus untuknya.
@param automaticColorInversion: berisi nilai boolean - sebuah automaticColorInversion diatur. True secara default.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Memungkinkan Anda menggunakan algoritma tambahan khusus untuk pengenalan font kecil.
Berguna untuk gambar dengan karakter berukuran kecil.
@param upscaleSmallFont: berisi nilai boolean - sebuah upscaleSmallFont diatur.

`PassportRecognitionSettings()`
:

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Mendeteksi gambar dengan teks putih pada latar belakang gelap/hitam dan secara otomatis memilih algoritma OCR khusus untuknya.
@param automaticColorInversion: berisi nilai boolean - sebuah automaticColorInversion diatur. True secara default.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Memungkinkan Anda menggunakan algoritma tambahan khusus untuk pengenalan font kecil.
Berguna untuk gambar dengan karakter berukuran kecil.
@param upscaleSmallFont: berisi nilai boolean - sebuah upscaleSmallFont diatur.

`ReceiptRecognitionSettings()`
:

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Mendeteksi gambar dengan teks putih pada latar belakang gelap/hitam dan secara otomatis memilih algoritma OCR khusus untuknya.
@param automaticColorInversion: berisi nilai boolean - sebuah automaticColorInversion diatur. True secara default.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Memungkinkan Anda menggunakan algoritma tambahan khusus untuk pengenalan font kecil.
Berguna untuk gambar dengan karakter berukuran kecil.
@param upscaleSmallFont: berisi nilai boolean - sebuah upscaleSmallFont diatur.

`RecognitionSettings()`
:
Pengaturan untuk pengenalan gambar.
Berisi elemen yang memungkinkan penyesuaian proses pengenalan.
    
    
Konstruktor default: set recognitionAreas null, linesFiltration false, autoSkew false, recognizeSingleLine false.

### Variabel kelas

`JAVA_CLASS_NAME`
:

### Metode

`set_allowed_characters(self, allowedCharacters: str)`
:
Set karakter yang diizinkan. Menentukan array karakter yang diizinkan untuk hasil pengenalan.
@param allowedCharacters: berisi string karakter.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Mendeteksi gambar dengan teks putih pada latar belakang gelap/hitam dan secara otomatis memilih algoritma OCR khusus untuknya.
@param automaticColorInversion: berisi nilai boolean - sebuah automaticColorInversion diatur. True secara default.

`set_detect_areas_mode(self, detectAreasMode: aspose.models.DetectAreasMode)`
:
Menentukan jenis jaringan saraf yang digunakan untuk deteksi area.
@param detectAreasMode: berisi nilai enum DetectAreasMode.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Mengatur daftar hitam untuk simbol pengenalan.
@param ignoredCharacters: Karakter yang dikecualikan dari pengenalan.

`set_language(self, language: aspose.models.Language)`
:
Mengatur bahasa yang digunakan untuk OCR.
Multi-bahasa (none) secara default.
@param language: berisi nilai enum Language.

`set_recognize_single_line(self, recognizeSingleLine: bool)`
:
Mengatur pengenalan gambar satu baris.
Dinonaktifkan (false) secara default.
Nonaktifkan semua langkah pemrosesan yang terkait dengan pemisahan menjadi baris.
Setel parameter ini ke true jika gambar Anda hanya berisi satu baris. Menonaktifkan pengaturan set_recognition_areas, sehingga semua pengaturan area akan diabaikan.
@param recognizeSingleLine: True untuk gambar satu baris

`set_threads_count(self, threadsCount: int)`
:
Mendapatkan atau mengatur jumlah thread untuk pemrosesan.
Secara default, 0 berarti gambar akan diproses dengan jumlah thread yang sama dengan jumlah prosesor Anda.
ThreadsCount = 1 berarti gambar akan diproses di thread utama.
@param threadsCount: jumlah thread yang akan dibuat untuk pengenalan paralel fragmen gambar.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Memungkinkan Anda menggunakan algoritma tambahan khusus untuk pengenalan font kecil.
Berguna untuk gambar dengan karakter berukuran kecil.
@param upscaleSmallFont: berisi nilai boolean - sebuah upscaleSmallFont diatur.



### Lihat Juga

* namespace [aspose](/ocr/python-java/aspose/)
* assembly [Aspose](/ocr/python-java/)