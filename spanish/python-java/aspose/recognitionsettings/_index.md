---
title: "RecognitionSettings"
second_title: "Referencia de la API de Aspose.OCR para Python vía Java"
description: 
type: docs
weight: 191
url: /es/python-java/aspose/recognitionsettings/
---

Módulo recognitionsettings
==========================

Clases
-------

`CarPlateRecognitionSettings()`
:

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`IDCardRecognitionSettings()`
:

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Detecta imágenes con texto blanco sobre fondo oscuro/negro y elige automáticamente un algoritmo OCR especial para ellas.
@param automaticColorInversion: contiene un valor booleano - se establece automaticColorInversion. True por defecto.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Permite usar algoritmos adicionales específicamente para el reconocimiento de fuentes pequeñas.
Útil para imágenes con caracteres de tamaño pequeño.
@param upscaleSmallFont: contiene un valor booleano - se establece upscaleSmallFont.

`InvoiceRecognitionSettings()`
:

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Detecta imágenes con texto blanco sobre fondo oscuro/negro y elige automáticamente un algoritmo OCR especial para ellas.
@param automaticColorInversion: contiene un valor booleano - se establece automaticColorInversion. True por defecto.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Permite usar algoritmos adicionales específicamente para el reconocimiento de fuentes pequeñas.
Útil para imágenes con caracteres de tamaño pequeño.
@param upscaleSmallFont: contiene un valor booleano - se establece upscaleSmallFont.

`PassportRecognitionSettings()`
:

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Detecta imágenes con texto blanco sobre fondo oscuro/negro y elige automáticamente un algoritmo OCR especial para ellas.
@param automaticColorInversion: contiene un valor booleano - se establece automaticColorInversion. True por defecto.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Permite usar algoritmos adicionales específicamente para el reconocimiento de fuentes pequeñas.
Útil para imágenes con caracteres de tamaño pequeño.
@param upscaleSmallFont: contiene un valor booleano - se establece upscaleSmallFont.

`ReceiptRecognitionSettings()`
:

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Detecta imágenes con texto blanco sobre fondo oscuro/negro y elige automáticamente un algoritmo OCR especial para ellas.
@param automaticColorInversion: contiene un valor booleano - se establece automaticColorInversion. True por defecto.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Permite usar algoritmos adicionales específicamente para el reconocimiento de fuentes pequeñas.
Útil para imágenes con caracteres de tamaño pequeño.
@param upscaleSmallFont: contiene un valor booleano - se establece upscaleSmallFont.

`RecognitionSettings()`
:
Configuraciones para el reconocimiento de imágenes.
Contiene elementos que permiten personalizar el proceso de reconocimiento.
    
    
Constructor predeterminado: establecer recognitionAreas a null, linesFiltration a false, autoSkew a false, recognizeSingleLine a false.

### Variables de clase

`JAVA_CLASS_NAME`
:

### Métodos

`set_allowed_characters(self, allowedCharacters: str)`
:
Conjunto de caracteres permitidos. Determina la matriz de caracteres permitidos para el resultado del reconocimiento.
@param allowedCharacters: contiene una cadena de caracteres.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Detecta imágenes con texto blanco sobre fondo oscuro/negro y elige automáticamente un algoritmo OCR especial para ellas.
@param automaticColorInversion: contiene un valor booleano - se establece automaticColorInversion. True por defecto.

`set_detect_areas_mode(self, detectAreasMode: aspose.models.DetectAreasMode)`
:
Determina el tipo de red neuronal utilizada para la detección de áreas.
@param detectAreasMode: contiene el valor del enum DetectAreasMode.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Establece la lista negra para los símbolos de reconocimiento.
@param ignoredCharacters: Caracteres excluidos del reconocimiento.

`set_language(self, language: aspose.models.Language)`
:
Establece el idioma utilizado para OCR.
Multilingüe (ninguno) por defecto.
@param language: contiene el valor enum Language.

`set_recognize_single_line(self, recognizeSingleLine: bool)`
:
Configura el reconocimiento de imagen de una sola línea.
Deshabilitado (false) por defecto.
Deshabilita todos los pasos de procesamiento asociados con la división en líneas.
Establezca este parámetro a true si su imagen contiene solo una línea. Deshabilita la configuración set_recognition_areas, por lo que todas las configuraciones de áreas serán ignoradas.
@param recognizeSingleLine: True para imagen de una sola línea

`set_threads_count(self, threadsCount: int)`
:
Obtiene o establece el número de hilos para el procesamiento.
Por defecto, 0 significa que la imagen se procesará con un número de hilos igual al número de procesadores.
ThreadsCount = 1 significa que la imagen se procesará en el hilo principal.
@param threadsCount: el número de hilos que se crearán para el reconocimiento paralelo de fragmentos de imagen.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Permite usar algoritmos adicionales específicamente para el reconocimiento de fuentes pequeñas.
Útil para imágenes con caracteres de tamaño pequeño.
@param upscaleSmallFont: contiene un valor booleano - se establece upscaleSmallFont.



### Ver también

* namespace [aspose](/ocr/python-java/aspose/)
* assembly [Aspose](/ocr/python-java/)