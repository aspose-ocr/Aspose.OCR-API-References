---
title: "LanguageDetectionLevel"
second_title: "مرجع Aspose.OCR لـ Java API"
description: "يحدد مستوى اكتشاف اللغة للتعرف على النص في صورة"
type: docs
weight: 26
url: /ar/java/com.aspose.ocr.models/languagedetectionlevel/
---

**Inheritance:**
java.lang.Object, java.lang.Enum
```
public enum LanguageDetectionLevel extends Enum<LanguageDetectionLevel>
```

يعرف مستوى اكتشاف اللغة للتعرف على النص في الصورة.
## الحقول

| الحقل | الوصف |
| --- | --- |
| [BY_PAGE](#BY-PAGE) | يكتشف لغة واحدة لكامل الصورة. |
| [BY_PARAGRAPH](#BY-PARAGRAPH) | يكتشف اللغة بشكل منفصل لكل فقرة. |
| [BY_WORD](#BY-WORD) | يكتشف اللغة بشكل منفصل لكل كلمة. |

### BY_PAGE {#BY-PAGE}
```
public static final LanguageDetectionLevel BY_PAGE
```


يكتشف لغة واحدة لكامل الصورة.

### BY_PARAGRAPH {#BY-PARAGRAPH}
```
public static final LanguageDetectionLevel BY_PARAGRAPH
```


يكتشف اللغة بشكل منفصل لكل فقرة.

### BY_WORD {#BY-WORD}
```
public static final LanguageDetectionLevel BY_WORD
```


يكتشف اللغة بشكل منفصل لكل كلمة.

