---
title: "Формат"
second_title: "Aspose.OCR для Java API Reference"
description: 
type: docs
weight: 30
url: /ru/java/com.aspose.ocr.models/format/
---

**Inheritance:**
java.lang.Object, java.lang.Enum
```
public enum Format extends Enum<Format>
```
## Поля

| Поле | Описание |
| --- | --- |
| [Csv](#Csv) | Сохраняет документ в виде файла CSV (.csv). |
| [Docx](#Docx) | Сохраняет результат как документ Office Open XML Word processing ML (без макросов). |
| [Epub](#Epub) | Сохраняет документ в виде файла EPUB. |
| [Html](#Html) | Сохраняет документ в виде файла HTML. |
| [Json](#Json) | Сохраняет результат как обычный текст, записанный в JavaScript object notation. |
| [Md](#Md) | Сохраняет документ в виде файла Markdown. |
| [Pdf](#Pdf) | Сохраняет результат в виде PDF (Adobe Portable Document) документа. |
| [PdfNoImg](#PdfNoImg) | Сохраняет документ в виде поискового PDF (Adobe Portable Document) документа без изображения. |
| [Rtf](#Rtf) | Сохраняет документ в виде файла rtf. |
| [Text](#Text) | Сохраняет результат в формате простого текста. |
| [Xlsx](#Xlsx) | Сохраняет результат в виде рабочей книги Excel (2007 и новее). |
| [Xml](#Xml) | Сохраняет результат в виде XML‑документа. |
| [hOcr](#hOcr) | Сохраняет документ в виде файла hOCR (.html). |

### Csv {#Csv}
```
public static final Format Csv
```


Сохраняет документ в виде файла CSV (.csv).

### Docx {#Docx}
```
public static final Format Docx
```


Сохраняет результат как документ Office Open XML Word processing ML (без макросов).

### Epub {#Epub}
```
public static final Format Epub
```


Сохраняет документ в виде файла EPUB.

### Html {#Html}
```
public static final Format Html
```


Сохраняет документ в виде файла HTML.

### Json {#Json}
```
public static final Format Json
```


Сохраняет результат как обычный текст, записанный в JavaScript object notation.

### Md {#Md}
```
public static final Format Md
```


Сохраняет документ в виде файла Markdown.

### Pdf {#Pdf}
```
public static final Format Pdf
```


Сохраняет результат в виде PDF (Adobe Portable Document) документа.

### PdfNoImg {#PdfNoImg}
```
public static final Format PdfNoImg
```


Сохраняет документ в виде поискового PDF (Adobe Portable Document) документа без изображения.

### Rtf {#Rtf}
```
public static final Format Rtf
```


Сохраняет документ в виде файла rtf.

### Text {#Text}
```
public static final Format Text
```


Сохраняет результат в формате простого текста.

### Xlsx {#Xlsx}
```
public static final Format Xlsx
```


Сохраняет результат в виде рабочей книги Excel (2007 и новее).

### Xml {#Xml}
```
public static final Format Xml
```


Сохраняет результат в виде XML‑документа.

### hOcr {#hOcr}
```
public static final Format hOcr
```


Сохраняет документ в виде файла hOCR (.html).

