---
title: "RecognitionSettings"
second_title: "Справочник API Aspose.OCR для Python через Java"
description: 
type: docs
weight: 191
url: /ru/python-java/aspose/recognitionsettings/
---

Модуль recognitionsettings
==========================

Классы
-------

`CarPlateRecognitionSettings()`
:

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`IDCardRecognitionSettings()`
:

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Обнаруживает изображения с белым текстом на тёмном/чёрном фоне и автоматически выбирает для них специальный алгоритм OCR.
@param automaticColorInversion: содержит булево значение — automaticColorInversion установлен. По умолчанию True.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Позволяет использовать дополнительные алгоритмы специально для распознавания мелкого шрифта.
Полезно для изображений с небольшими символами.
@param upscaleSmallFont: содержит булево значение — upscaleSmallFont установлен.

`InvoiceRecognitionSettings()`
:

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Обнаруживает изображения с белым текстом на тёмном/чёрном фоне и автоматически выбирает для них специальный алгоритм OCR.
@param automaticColorInversion: содержит булево значение — automaticColorInversion установлен. По умолчанию True.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Позволяет использовать дополнительные алгоритмы специально для распознавания мелкого шрифта.
Полезно для изображений с небольшими символами.
@param upscaleSmallFont: содержит булево значение — upscaleSmallFont установлен.

`PassportRecognitionSettings()`
:

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Обнаруживает изображения с белым текстом на тёмном/чёрном фоне и автоматически выбирает для них специальный алгоритм OCR.
@param automaticColorInversion: содержит булево значение — automaticColorInversion установлен. По умолчанию True.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Позволяет использовать дополнительные алгоритмы специально для распознавания мелкого шрифта.
Полезно для изображений с небольшими символами.
@param upscaleSmallFont: содержит булево значение — upscaleSmallFont установлен.

`ReceiptRecognitionSettings()`
:

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Обнаруживает изображения с белым текстом на тёмном/чёрном фоне и автоматически выбирает для них специальный алгоритм OCR.
@param automaticColorInversion: содержит булево значение — automaticColorInversion установлен. По умолчанию True.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Позволяет использовать дополнительные алгоритмы специально для распознавания мелкого шрифта.
Полезно для изображений с небольшими символами.
@param upscaleSmallFont: содержит булево значение — upscaleSmallFont установлен.

`RecognitionSettings()`
:
Настройки распознавания изображения.
Содержит элементы, позволяющие настраивать процесс распознавания.
    
    
Конструктор по умолчанию: установить recognitionAreas в null, linesFiltration в false, autoSkew в false, recognizeSingleLine в false.

### Переменные класса

`JAVA_CLASS_NAME`
:

### Методы

`set_allowed_characters(self, allowedCharacters: str)`
:
Набор разрешённых символов. Определяет массив символов, допускаемых в результате распознавания.
@param allowedCharacters: содержит строку символов.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Обнаруживает изображения с белым текстом на тёмном/чёрном фоне и автоматически выбирает для них специальный алгоритм OCR.
@param automaticColorInversion: содержит булево значение — automaticColorInversion установлен. По умолчанию True.

`set_detect_areas_mode(self, detectAreasMode: aspose.models.DetectAreasMode)`
:
Определяет тип нейронной сети, используемой для обнаружения областей.
@param detectAreasMode: содержит значение перечисления DetectAreasMode.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Устанавливает черный список символов распознавания.
@param ignoredCharacters: Символы, исключённые из распознавания.

`set_language(self, language: aspose.models.Language)`
:
Устанавливает язык, используемый для OCR.
Многоязычный (none) по умолчанию.
@param language: содержит значение перечисления Language.

`set_recognize_single_line(self, recognizeSingleLine: bool)`
:
Устанавливает распознавание изображения в одну строку.
Отключено (false) по умолчанию.
Отключить все этапы обработки, связанные с разбиением на строки.
Установите этот параметр в true, если ваше изображение содержит только одну строку. Отключает настройки set_recognition_areas, поэтому все настройки областей будут игнорироваться.
@param recognizeSingleLine: True для однострочного изображения

`set_threads_count(self, threadsCount: int)`
:
Получает или задает количество потоков для обработки.
По умолчанию 0 означает, что изображение будет обрабатываться с количеством потоков, равным количеству процессоров.
ThreadsCount = 1 означает, что изображение будет обрабатываться в основном потоке.
@param threadsCount: количество потоков, которые будут созданы для параллельного распознавания фрагментов изображения.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Позволяет использовать дополнительные алгоритмы специально для распознавания мелкого шрифта.
Полезно для изображений с небольшими символами.
@param upscaleSmallFont: содержит булево значение — upscaleSmallFont установлен.



### См. также

* namespace [aspose](/ocr/python-java/aspose/)
* assembly [Aspose](/ocr/python-java/)