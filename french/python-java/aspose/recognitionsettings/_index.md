---
title: "RecognitionSettings"
second_title: "Référence de l'API Aspose.OCR pour Python via Java"
description: 
type: docs
weight: 191
url: /fr/python-java/aspose/recognitionsettings/
---

Module recognitionsettings
==========================

Classes
-------

`CarPlateRecognitionSettings()`
:

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`IDCardRecognitionSettings()`
:

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Détecte les images avec du texte blanc sur fond sombre/noir et choisit automatiquement un algorithme OCR spécial pour celles‑ci.
@param automaticColorInversion: contient une valeur booléenne – automaticColorInversion est définie. True par défaut.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Vous permet d’utiliser des algorithmes supplémentaires spécifiquement pour la reconnaissance de petites polices.
Utile pour les images contenant des caractères de petite taille.
@param upscaleSmallFont: contient une valeur booléenne – upscaleSmallFont est définie.

`InvoiceRecognitionSettings()`
:

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Détecte les images avec du texte blanc sur fond sombre/noir et choisit automatiquement un algorithme OCR spécial pour celles‑ci.
@param automaticColorInversion: contient une valeur booléenne – automaticColorInversion est définie. True par défaut.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Vous permet d’utiliser des algorithmes supplémentaires spécifiquement pour la reconnaissance de petites polices.
Utile pour les images contenant des caractères de petite taille.
@param upscaleSmallFont: contient une valeur booléenne – upscaleSmallFont est définie.

`PassportRecognitionSettings()`
:

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Détecte les images avec du texte blanc sur fond sombre/noir et choisit automatiquement un algorithme OCR spécial pour celles‑ci.
@param automaticColorInversion: contient une valeur booléenne – automaticColorInversion est définie. True par défaut.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Vous permet d’utiliser des algorithmes supplémentaires spécifiquement pour la reconnaissance de petites polices.
Utile pour les images contenant des caractères de petite taille.
@param upscaleSmallFont: contient une valeur booléenne – upscaleSmallFont est définie.

`ReceiptRecognitionSettings()`
:

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Détecte les images avec du texte blanc sur fond sombre/noir et choisit automatiquement un algorithme OCR spécial pour celles‑ci.
@param automaticColorInversion: contient une valeur booléenne – automaticColorInversion est définie. True par défaut.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Vous permet d’utiliser des algorithmes supplémentaires spécifiquement pour la reconnaissance de petites polices.
Utile pour les images contenant des caractères de petite taille.
@param upscaleSmallFont: contient une valeur booléenne – upscaleSmallFont est définie.

`RecognitionSettings()`
:
Paramètres pour la reconnaissance d’image.
Contient des éléments qui permettent de personnaliser le processus de reconnaissance.
    
    
Constructeur par défaut : définir recognitionAreas à null, linesFiltration à false, autoSkew à false, recognizeSingleLine à false.

### variables de classe

`JAVA_CLASS_NAME`
:

### Méthodes

`set_allowed_characters(self, allowedCharacters: str)`
:
Ensemble de caractères autorisés. Détermine le tableau des caractères autorisés pour le résultat de reconnaissance.
@param allowedCharacters: contient une chaîne de caractères.

`set_automatic_color_inversion(self, automaticColorInversion: bool)`
:
Détecte les images avec du texte blanc sur fond sombre/noir et choisit automatiquement un algorithme OCR spécial pour celles‑ci.
@param automaticColorInversion: contient une valeur booléenne – automaticColorInversion est définie. True par défaut.

`set_detect_areas_mode(self, detectAreasMode: aspose.models.DetectAreasMode)`
:
Détermine le type de réseau neuronal utilisé pour la détection des zones.
@param detectAreasMode: contient la valeur de l'énumération DetectAreasMode.

`set_ignored_characters(self, ignoredCharacters: str)`
:
Définit la liste noire pour les symboles de reconnaissance.
@param ignoredCharacters: caractères exclus de la reconnaissance.

`set_language(self, language: aspose.models.Language)`
:
Définit la langue utilisée pour l’OCR.
Multilingue (aucune) par défaut.
@param language: contient la valeur d’enumération Language.

`set_recognize_single_line(self, recognizeSingleLine: bool)`
:
Définit la reconnaissance d'image à ligne unique.
Désactivé (false) par défaut.
Désactive toutes les étapes de traitement associées à la division en lignes.
Définissez ce paramètre sur true si votre image ne contient qu'une seule ligne. Désactive les paramètres set_recognition_areas, de sorte que tous les paramètres de zones seront ignorés.
@param recognizeSingleLine: True pour une image à ligne unique

`set_threads_count(self, threadsCount: int)`
:
Obtient ou définit le nombre de threads pour le traitement.
Par défaut, 0 signifie que l’image sera traitée avec un nombre de threads égal à celui de vos processeurs.
ThreadsCount = 1 signifie que l’image sera traitée dans le thread principal.
@param threadsCount: le nombre de threads qui seront créés pour la reconnaissance parallèle des fragments d’image.

`set_upscale_small_font(self, upscaleSmallFont: bool)`
:
Vous permet d’utiliser des algorithmes supplémentaires spécifiquement pour la reconnaissance de petites polices.
Utile pour les images contenant des caractères de petite taille.
@param upscaleSmallFont: contient une valeur booléenne – upscaleSmallFont est définie.



### Voir aussi

* namespace [aspose](/ocr/python-java/aspose/)
* assembly [Aspose](/ocr/python-java/)