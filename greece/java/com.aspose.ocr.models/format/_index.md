---
title: "Μορφή"
second_title: "Αναφορά API του Aspose.OCR για Java"
description: 
type: docs
weight: 30
url: /el/java/com.aspose.ocr.models/format/
---

**Inheritance:**
java.lang.Object, java.lang.Enum
```
public enum Format extends Enum<Format>
```
## Πεδία

| Πεδίο | Περιγραφή |
| --- | --- |
| [Csv](#Csv) | Αποθηκεύει το έγγραφο ως αρχείο CSV (.csv). |
| [Docx](#Docx) | Αποθηκεύει το αποτέλεσμα ως έγγραφο Office Open XML Word processing ML (χωρίς μακροεντολές). |
| [Epub](#Epub) | Αποθηκεύει το έγγραφο ως αρχείο EPUB. |
| [Html](#Html) | Αποθηκεύει το έγγραφο ως αρχείο HTML. |
| [Json](#Json) | Αποθηκεύει το αποτέλεσμα ως απλό κείμενο γραμμένο σε μορφή JavaScript Object Notation. |
| [Md](#Md) | Αποθηκεύει το έγγραφο ως αρχείο Markdown. |
| [Pdf](#Pdf) | Αποθηκεύει το αποτέλεσμα ως έγγραφο PDF (Adobe Portable Document). |
| [PdfNoImg](#PdfNoImg) | Αποθηκεύει το έγγραφο ως Αναζητήσιμο PDF (Adobe Portable Document) χωρίς εικόνα. |
| [Rtf](#Rtf) | Αποθηκεύει το έγγραφο ως αρχείο rtf. |
| [Text](#Text) | Αποθηκεύει το αποτέλεσμα σε μορφή απλού κειμένου. |
| [Xlsx](#Xlsx) | Αποθηκεύει το αποτέλεσμα ως έγγραφο εργασίας Excel (2007 και νεότερο). |
| [Xml](#Xml) | Αποθηκεύει το αποτέλεσμα ως έγγραφο XML. |
| [hOcr](#hOcr) | Αποθηκεύει το έγγραφο ως αρχείο hOCR (.html). |

### Csv {#Csv}
```
public static final Format Csv
```


Αποθηκεύει το έγγραφο ως αρχείο CSV (.csv).

### Docx {#Docx}
```
public static final Format Docx
```


Αποθηκεύει το αποτέλεσμα ως έγγραφο Office Open XML Word processing ML (χωρίς μακροεντολές).

### Epub {#Epub}
```
public static final Format Epub
```


Αποθηκεύει το έγγραφο ως αρχείο EPUB.

### Html {#Html}
```
public static final Format Html
```


Αποθηκεύει το έγγραφο ως αρχείο HTML.

### Json {#Json}
```
public static final Format Json
```


Αποθηκεύει το αποτέλεσμα ως απλό κείμενο γραμμένο σε μορφή JavaScript Object Notation.

### Md {#Md}
```
public static final Format Md
```


Αποθηκεύει το έγγραφο ως αρχείο Markdown.

### Pdf {#Pdf}
```
public static final Format Pdf
```


Αποθηκεύει το αποτέλεσμα ως έγγραφο PDF (Adobe Portable Document).

### PdfNoImg {#PdfNoImg}
```
public static final Format PdfNoImg
```


Αποθηκεύει το έγγραφο ως Αναζητήσιμο PDF (Adobe Portable Document) χωρίς εικόνα.

### Rtf {#Rtf}
```
public static final Format Rtf
```


Αποθηκεύει το έγγραφο ως αρχείο rtf.

### Text {#Text}
```
public static final Format Text
```


Αποθηκεύει το αποτέλεσμα σε μορφή απλού κειμένου.

### Xlsx {#Xlsx}
```
public static final Format Xlsx
```


Αποθηκεύει το αποτέλεσμα ως έγγραφο εργασίας Excel (2007 και νεότερο).

### Xml {#Xml}
```
public static final Format Xml
```


Αποθηκεύει το αποτέλεσμα ως έγγραφο XML.

### hOcr {#hOcr}
```
public static final Format hOcr
```


Αποθηκεύει το έγγραφο ως αρχείο hOCR (.html).

